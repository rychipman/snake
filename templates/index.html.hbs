<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>{{title}}</title>
  </head>
  <body>
    <h1>{{title}}</h1>
    <h3>Here are your scores:</h3>
    <ul>
      {{#each scores}}
      <li>{{score}} ({{email}})</li>
      {{/each}}
    </ul>
  </body>
</html>
